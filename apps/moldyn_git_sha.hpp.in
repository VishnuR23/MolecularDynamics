// Generated by apps/GitSha.cmake at build time -- do not edit.
//
// Holds the git revision the binary was built from, as reported by
// `git describe --always --dirty`. Regenerated on every build; the file
// is only rewritten when the revision changes, so an unchanged revision
// does not force a recompile.
#pragma once

#define MOLDYN_GIT_SHA "@MOLDYN_GIT_SHA@"
